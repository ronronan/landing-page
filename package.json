{
  "name": "landing-page",
  "private": true,
  "version": "0.0.1",
  "description": "Landing page for ronanmorel.fr",
  "license": "MIT",
  "scripts": {
    "dev": "stencil build --dev --watch --serve",
    "start": "stencil build --dev --watch --serve",
    "build": "stencil build"
  },
  "devDependencies": {
    "@stencil-community/router": "^1.0.2",
    "@stencil/core": "2.13.0",
    "puppeteer": "^10.4.0"
  }
}
