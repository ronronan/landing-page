{
  "name": "portfolio",
  "version": "0.0.1",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "deploy": "rclone sync ./dist mys3:ronanmorel.fr --progress --s3-acl=public-read"
  },
  "devDependencies": {
    "vite": "^4.0.4"
  }
}
